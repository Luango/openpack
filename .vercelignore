# Dev-only files — not part of the static site Vercel serves.
serve.py
docs
.claude
# the 9.7MB multi-view design sheet is a REFERENCE only (never loaded at runtime)
assets/pack design sheet.png
# NB: assets/pack.png IS shipped — the 3D carousel (src/select3d.js) uses it as the
# hi-res pack texture; the SVG tear-pack still uses the small assets/pack.webp.
