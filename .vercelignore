# Dev-only files — not part of the static site Vercel serves.
serve.py
docs
.claude
# the 9.7MB multi-view design sheet is a REFERENCE only (never loaded at runtime)
assets/pack design sheet.png
# the 2.7MB PNG is the editable SOURCE; runtime ships the WebPs instead:
#   • assets/pack.webp    — small (134KB), the SVG tear-pack LCP
#   • assets/pack-hi.webp — hi-res (~270KB), the 3D carousel texture
assets/pack.png
